import { useState } from 'react'
import { searchTitles } from './api/watchingWheel'
import SearchBar from './components/SearchBar'

function App() {
  const [results, setResults] = useState([])
  const [status, setStatus] = useState('idle')
  const er

  return (

  )
}

export default App
